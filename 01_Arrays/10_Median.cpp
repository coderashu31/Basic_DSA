// Find Median of the given Array

// Example 1:
// Input: [2,4,1,3,5]
// Output: 3

// Example 2:
// Input: [2,5,1,7]
// Output: 3.5

#include<bits/stdc++.h>
using namespace std;

int median(int arr[], int n){
    arr.sort(ar)
    for(int i = 0 ; i < n ; i++){
        if (n % 2 == 0){
            int index_1 = n/2 - 1 ;
            int index_2 = n/2 ;


        }
    }
}

int main(){
    int arr[n];
    int n = sizeof(arr)/sizeof(arr[0]);
    
    for(int i=0; i < n;  i++){
        cin>>arr[i];
    }
    
    cout<<"The median is: "<<"\n";
    get_median(arr,n);
    return 0;
}